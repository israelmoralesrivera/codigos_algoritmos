#include <bits/stdc++.h>

using namespace std;


int main()
{
    int n, k;
    cin >> n >> k;
    int entrada[n];
    for(int i=0; i<n; i++){
        cin >> k;
    }
    int res=0;
    for(int i=0; i<n;i++){
        if(entrada[i] > res)
    }

	return 0;
}
